
sources:
  - name: raw
    schema: gz_raw_data
    description: "Dataset brut BigQuery avec alias simplifié"
    tables:
      - name: raw_gz_sales
        alias: sales
      - name: raw_gz_product
        alias: product
      - name: raw_gz_ship
        alias: ship